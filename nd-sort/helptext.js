export const helptext = `nd-sort [SORT]

Language sensitive sort of newline delimited JSON or text streamed from STDIN

Use
  nd-sort [SORTFX] [--on key] [--numeric] [--reverse] [--lang=lang] < INPUT
`;
